<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }}</title>
        {%- block extrahead %}
        {%- endblock %}
    </head>
    <body>
        <header>
            <h1>{{ title | safe }}</h1>
            {%- block extraheader %}
            {%- endblock %}
        </header>
        <main>
            {{ contents | safe }}
        </main>
    </body>
</html>