{% extends 'base.njk' %}

{%- block contents %}
<main>
    <h2>{{ title }}</h2>
    <ul>
    {%- for c in concepts %}
    <li><a href="{{ c.node.permalink|url }}">{{ c.node.id }}</a> ({{ c.count }} definitions)</li>
    {%- endfor %}
    </ul>
</main>
{%- endblock %}
